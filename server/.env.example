# 🚗 รถเช่าอุดรธานี รุ่งโรจน์คาร์เร้นท์ - Server Environment Variables
# 📋 คัดลอกไฟล์นี้เป็น .env และใส่ค่าจริง

# Server Configuration
NODE_ENV=development
PORT=5000

# ✉️ Email Configuration (สำหรับส่งอีเมล์ยืนยันการจอง)
# วิธีใช้ Gmail App Password: https://support.google.com/accounts/answer/185833
# 1. ไปที่ Google Account → Security → 2-Step Verification (เปิดใช้งาน)
# 2. ไปที่ App passwords → เลือก Mail → สร้าง password 16 หลัก
# 3. นำ password มาใส่ใน MAIL_PASS
MAIL_USER=your_email@gmail.com
MAIL_PASS=your_gmail_app_password_16_characters

# 🗄️ MongoDB (ถ้าต้องการ database เพิ่มเติม - ไม่บังคับ)
MONGO_URI=

# 🔐 JWT Secret (สำหรับ custom authentication - ไม่บังคับ)
JWT_SECRET=your-secret-key-at-least-32-characters-long

# 📊 Supabase (สำหรับ server-side operations - ไม่บังคับ)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
